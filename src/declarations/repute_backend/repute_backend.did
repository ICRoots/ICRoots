type InitArgs = record {
  admin : opt principal;
  loans : opt principal;
  event_bus : opt principal;
};

service : (opt InitArgs) -> {
  get_level : (principal) -> (nat) query;
  set_level : (principal, nat) -> ();
}
